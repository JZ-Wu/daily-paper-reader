---
title: "OR-Action: Multi-Role Video Understanding with Fine-Grained Actions"
title_zh: "OR-Action: 基于细粒度动作的多角色视频理解"
authors: "Felix Tristram, Ege Özsoy, Christian Benz, Marcel Walch, Ghazal Ghazaei, Nassir Navab"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13332v1"
tags: ["query:tf-vl-ag"]
score: 9.0
evidence: 手术室视频的细粒度动作理解及动作中心基准
tldr: 手术室活动细粒度理解面临杂乱遮挡等挑战，现有场景图方法缺乏时序建模，难以生成连贯动作。本文构建首个动作中心基准，利用场景图状态变化蒸馏密集动作段，并引入纯视觉时序模型，性能显著优于图方法。进一步提出多到单视角特征对齐策略，提升单视角多角色动作识别，减轻对大量第一人称视频的依赖。该基准和模型为手术室时序理解提供了新评估标准与解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13332v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 360, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13332v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1258, \"height\": 573, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13332v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1275, \"height\": 346, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13332v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1111, \"height\": 593, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13332v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1226, \"height\": 516, \"label\": \"Table\"}]"
motivation: 现有手术室场景图方法缺乏时序建模，难以将帧级关系预测转化为时序细粒度动作。
method: 本文构建动作中心基准，蒸馏场景图变化生成动作段，并设计纯视觉时序模型及多到单视角对齐策略。
result: 纯视觉时序模型性能显著优于图方法，多到单视角特征对齐提升单视角多角色动作识别。
conclusion: 该工作建立了手术室时序动作理解基准，验证了纯视觉模型的有效性，并减少了对第一人称视频的依赖。
---

## 摘要
对手术室（OR）活动的细粒度理解有助于实现工作流感知的辅助，但由于环境杂乱、遮挡和传感限制，这仍然很困难。目前对此类环境建模的主流方法是场景图，作为一种可解释的OR交互表示。然而，在没有显式时间建模的情况下，将其逐帧的关系预测转化为时间上延展的细粒度动作十分具有挑战性。为了对现有OR理解方法进行原则性的时间评估，我们基于一个公开的自视角-外视角OR数据集，构建了首个以动作为中心的基准测试，通过定义细粒度的多角色动作分类体系，并利用从真值场景图状态变化中蒸馏信息来生成密集的动作片段。在该基准上的实验表明，当前的场景图预测方法难以建模时间结构，即使通过图神经网络加入显式建模也是如此。因此，我们提出一种仅使用视觉的时间模型，当使用所有可用的自视角视频作为输入时，其性能显著优于基于图的方法。在该模型基础上，我们还引入了一种新颖的多视图到单视图特征对齐策略，提升了单视图在多角色动作识别上的表现，从而减少了对大量自视角视频采集的需求。基准测试和代码将在论文接收后发布。

## Abstract
Fine-grained understanding of operating room (OR) activity could enable workflow-aware assistance, yet remains difficult due to clutter, occlusions, and limited sensing. The prevailing approach to model this environment is scene graphs as an interpretable representation of OR interactions. Converting their frame-wise relational predictions into temporally extended, fine-grained actions however, is challenging without explicit temporal modeling. To enable a principled temporal evaluation of current OR understanding methods, we introduce the first action-centric benchmark built on a publicly available ego-exocentric OR dataset by defining a fine-grained, multi-role action taxonomy and generating dense action segments via distillation from ground-truth scene graph state changes. Experiments on this benchmark show that current scene graph prediction methods struggle to model temporal structure, even when adding explicit modeling through Graph Neural Networks. We therefore introduce a vision-only temporal model that outperforms graph-based methods significantly when using all available egocentric video as input. Building on this model we also introduce a novel multi- to single-view feature alignment strategy that improves single-view performance on multi-role action recognition, mitigating the need for extensive egocentric video capture. Benchmark and code will be released upon acceptance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究背景**：手术室是一个多人员、多器械、高动态、安全关键的复杂环境。对其外部场景（即不同于内窥镜视角的整体环境）进行可靠感知，有望实现上下文感知的安全检查、工作流感知界面及自动化文档记录。
*   **核心问题**：当前主流方法（语义场景图）侧重于单时间点、静态的关系解析，缺乏对时间结构的显式建模。这导致在将逐帧的场景图预测结果，转化为具有时间延续性的、细粒度的角色动作（如器械拿起、传递）时，表现脆弱且不连贯，尤其难以处理长尾关系、遮挡和依赖时序顺序的动作语义。
*   **研究意义**：弥合“空间关系解析”与“时序动作理解”之间的鸿沟，将外部手术室环境的理解水平推向更贴近临床实际，能够处理多角色动态交互的成熟阶段。

### 2. 论文提出的方法论

论文主要包含三部分贡献：一个全新的动作基准数据集、一个纯视觉的时序模型以及一种多视图到单视图的特征对齐策略。

*   **OR-Action基准构建**：
    *   **数据来源**：基于公开的EgoExOR数据集，该数据集包含自视角（多角色）和外视角视频及场景图真值标注。
    *   **规则化映射器**：设计了一个由规则驱动的映射器，将逐帧的、由<主语, 谓语, 宾语>三元组构成的场景图，蒸馏成连续的动作片段。
        *   **状态规则**：当特定布尔条件（如 `麻醉师，注视，生命体征监视器`）持续为真时，触发持续性动作（如“麻醉师监测生命体征”）。
        *   **事件规则**：检测关系的变化点（如 `助理，持有，手术刀` 的开始），并将其扩展为包含动作前兆和后续运动的短期动作片段。
        *   **时间证据聚合**：通过统计“近期”或“以往”是否发生过某事件来增强鲁棒性，并解决如器械传递、返回等跨角色交互的检测。
*   **纯视觉多角色时序动作识别模型**：
    *   **输入**：来自多个角色视角的同步视频片段。
    *   **骨干网络**：使用冻结的视频基础模型（VJEPA2）提取时空特征标记 (`z(r)`)。
    *   **角色池化器**：使用一组可学习的查询Token (`RP_Q`)，通过交叉注意力机制，将长视频特征序列压缩为每个角色单个时间点的凝练特征 (`u(r)_t`)。
    *   **逐帧跨角色融合与分类**：在每一帧，将不同角色池化后的特征拼接，通过第二个注意力池化器 (`RC`)生成针对每个目标角色的融合特征 (`h(r)_t`)。最后，通过两个独立的线性头分别预测动作类别和活跃/空闲状态。
*   **多视图到单视图特征对齐策略**：
    *   **动机**：在实际部署中可能只有一个视角的视频，但仍需进行多角色动作预测。
    *   **流程**：首先训练一个使用所有视角的“多角色教师模型”。然后，训练一个“单视图学生模型”，其输入仅为某一固定角色的视频。
    *   **对齐损失**：在冻结教师模型后，通过L1损失最小化学生模型特征序列与教师模型生成的多角色融合特征序列之间的差异，强制学生模型从单视角视频中提取出蕴含多角色上下文的表征。
    *   **联合训练**：最终目标函数为对齐损失与监督动作分类损失的加权和。

### 3. 实验设计

*   **数据集与基准**：
    *   使用公开的**EgoExOR**数据集，并基于此构建的**OR-Action**基准，共包含**78个**动作类别，**1295个**动作片段。
    *   采用与EgoExOR一致的官方训练/验证集划分。
*   **对比方法**：
    *   **基于场景图的基线方法**：
        1.  `EgoExOR + rule mapping`：将自定义的规则映射器直接应用于EgoExOR模型预测的场景图。
        2.  `EgoExOR + GNN`：在图神经网络中显式建模时间关系，但输入为EgoExOR预测的场景图。
        3.  `GT + GNN`：作为理论上限，在图神经网络中使用数据集提供的真实场景图。
    *   **本文提出的方法**：
        1.  `Ours (all roles)`：使用所有可用视角视频的纯视觉模型。
        2.  不同单视角输入下的模型（含视图对齐策略的消融实验）。
*   **评估指标**：
    *   **帧准确率（含/不含空闲类别）**：整体分类正确率。
    *   **编辑分数**：评估预测动作序列顺序和过分割/欠分割错误的指标。
    *   **片段F1分数（F1@0.1， 0.25， 0.5）**：在不同时间交并比阈值下，评估动作片段边界和类别一致性。

### 4. 资源与算力

*   文中未明确提及进行实验所使用的具体硬件配置、GPU型号、数量或训练时长。

### 5. 实验数量与充分性

*   **实验组数**：**5组以上**。包含基准对比实验（4种方法）、消融实验（单独预测头、不同视角输入）以及验证特征对齐策略有效性的实验。
*   **充分性评估**：实验设计较为全面和客观。
    *   **对比充分**：与多种基线方法（规则映射、图神经网络、理论上限）进行了对比。
    *   **消融客观**：系统地验证了模型关键组件（分离的活跃/动作预测头）和不同输入条件（自视角、外视角、不同角色单视角）对性能的影响。
    *   **评估多维**：使用了帧级、序列级和片段级的多项指标，能够从不同维度评估模型的时序理解能力。

### 6. 论文的主要结论与发现

*   **场景图方法的时序局限性**：直接将逐帧的场景图预测转化为时序动作品质很差，即使引入图神经网络进行显式时序建模，由于源场景图预测质量及类别不平衡等问题，性能提升仍十分有限，证明了仅依靠静态关系解析的不足。
*   **纯视觉模型的显著优势**：提出的纯视觉时序模型在使用所有自视角视频时，性能大幅超越所有基于场景图的预测方法，并在多项指标上接近甚至超越使用真值场景图的理论上限，尤其在捕捉短暂的动作和提升片段F1分数方面表现突出。
*   **视角的关键作用**：
    *   自视角（佩戴者视角）信息对于可靠动作理解至关重要，其性能远超仅使用固定外视角相机。
    *   不同角色的自视角信息量差异巨大，例如，观察范围有限或不参与核心操作的“巡回护士”视角性能较弱。
*   **特征对齐策略的有效性**：提出的多到单视图特征对齐策略能有效提升“外科医生”和“助理”等关键角色的单视角动作识别性能，尤其是在准确率和编辑分数上，证明该策略可迫使模型从单一视角中挖掘更多上下文线索。

### 7. 优点（方法论与实验设计的亮点）

*   **任务与基准的创新性**：首次将外部手术室环境的理解从静态场景图解析，推进到更具挑战和临床实用价值的细粒度、多角色时序动作理解任务上，并为此提供了系统的基准。
*   **干净且可复用的标注生成范式**：利用规则从现有场景图数据中蒸馏动作标注的方法，为解决标注成本高昂的密集视频任务提供了一种极具参考价值的范式。
*   **务实且高效的技术方案**：现有多视图特征对齐策略考虑了实际部署中摄像头配置可能不完整的限制，通过知识蒸馏的方式提升单视角模型的性能，降低了数据采集门槛，具有很好的应用导向。
*   **对比实验结果深刻**：通过对比真值图模型与预测图模型的性能差距，清晰、深刻地揭示了当前场景图方法的瓶颈在于预测阶段而非图表征能力本身，为后续研究指明了方向。

### 8. 不足与局限

*   **标注质量的潜在偏差**：动作标签是通过规则从场景图真值中生成的，其质量直接受限于原始场景图标注的粒度和准确性。规则本身的定义（如“近期”窗口大小）也引入了人工设定的先验。
*   **视角对齐策略的适用性边界**：特征对齐策略对“巡回护士”这种难以观察全局动作的视角提升不大，表明该方法的有效性依赖于源视角本身能够捕捉到一定程度的有效信息，其泛化边界未被充分定义。
*   **视觉上相似动作的混淆**：论文承认，纯视觉模型对如“定位显微镜视野”和“检查手术步骤”等缺乏显著视觉差异的动作仍然难以区分，表明仅靠外观信息存在根本性局限。
*   **计算资源和实施细节缺失**：未提供使用视频基础模型和进行多视角训练的算力开销、训练时长等关键实施细节，使得他人复现和评估其资源需求时存在困难。


（完）
